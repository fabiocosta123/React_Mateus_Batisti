const ExecuteFunctions = ({ ShowMessage }) => {
  return (
    <div>
      <button onClick={ShowMessage}>Clique aqui para executar a função</button>
    </div>
  );
};

export default ExecuteFunctions;
